import {StyleSheet} from 'react-native';
// import {quickTheme} from '../../../theme/QuickThemePaperProvider';

export const styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    // padding: quickTheme.espacamento.big,
    backgroundColor: '#F7F7F7',
  },
  input: {
    width: '100%',
    backgroundColor: '#F7F7F7',
  },
  gap: {
    // marginVertical: quickTheme.espacamento.soma.small,
  },
  botao: {
    width: '100%',
    // marginTop: quickTheme.espacamento.big,
    // borderRadius: quickTheme.arredondamento.normal,
  },
});
